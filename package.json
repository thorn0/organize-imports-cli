{
  "name": "organize-imports-cli",
  "version": "0.7.0",
  "description": "VS Code's 'Organize imports' executable from command line",
  "bin": "./cli.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/thorn0/organize-imports-cli.git"
  },
  "author": "Georgii Dolzhykov",
  "license": "MIT",
  "dependencies": {
    "chalk": "^3.0.0",
    "editorconfig": "^0.15.3",
    "ts-morph": "^6.0.2",
    "tsconfig": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^13.1.2",
    "escape-string-regexp": "^2.0.0",
    "jest": "^24.9.0",
    "strip-ansi": "^6.0.0"
  },
  "scripts": {
    "all-checks": "npm test && npm run typecheck",
    "test": "jest",
    "typecheck": "tsc --checkJs --allowJs --noEmit --strict cli.js"
  }
}
